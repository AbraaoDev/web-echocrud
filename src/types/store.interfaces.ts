export interface Store {
  id: string
  name: string
  storeNumber: string
  storeName: string
  address: string
  city: string
  state: string
  zipCode: string
  number: string
  establishmentId: string
}
